{
    "tasks": [
        {
            "type": "shell",
            "label": "clang++ compile single file",
            "command": "/usr/bin/clang++",
            "args": [
                "-g",
                "-I${fileDirname}/..",
                "${file}",
                "-o",
                "${fileDirname}/a.out",
                "-DTEST_FT"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}